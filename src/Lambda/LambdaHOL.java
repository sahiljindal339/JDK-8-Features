package Lambda;

@FunctionalInterface
public interface LambdaHOL {
	// when we have to Use Lambda we need function
	// that we borrow from interface
	// Interface with one function is called Function Interface
	// Diff b/w function and method..........?
	void lambdaHOLv2(String val);
}
